# 일정 + 학생참여 상세 설계 (스샷 · 구글시트 분석 결과)

## 0. 두 자료가 하나의 흐름이었음

**스샷** = `2024 자기개발시기 3학년 특별 학사일정 및 고입 전형 일정`
**시트** = `2024 자기개발시기 출석부 (12월)`

시트의 체크 대상 4개 날짜가 스샷의 일정과 정확히 일치합니다:

| 날짜 | 스샷의 일정 | 시트 참여자 | 미참여 |
|---|---|---|---|
| 12/6(금) | 영화관람 (가양 롯데시네마 / 11시 전 시작) | 216 | 11 |
| 12/9(월) | 쇼콜라티에 만들기 (1,2교시 앞반 / 3,4교시 뒷반) | 214 | 13 |
| 12/10(화) | 난타 공연 (홍대 전용관 / 11:00) | 215 | 12 |
| 12/19(목) | 도자기 체험 (각반 교실) | 197 | **30** |

즉 만들 것은 이겁니다:

```
부장이 일정 등록  →  "학생 참여 체크 필요" 켬
   →  담임이 반별로 참여/불참 입력
      →  반별 집계 → 학년 총원 → 불참자 명단  (자동)
```

지금은 이 흐름이 **엑셀 시트 12개 탭 + 수식**으로 굴러가고 있습니다.

---

## 1. 학교 정보 (스샷에서 확정된 사실)

- **중학교**, 3학년 대상
- **3학년만 10개 반** (3-1 ~ 3-10), 3학년 인원 약 227명
- 반당 22~24명
- 앞서 리뷰에서 "초/중/고 어디냐" 질문했던 건 **중학교로 확정**

---

## 2. 일정표에서 도출된 요구사항

### 2-1. 시간 단위가 "시:분"이 아니라 **교시**  ← 가장 큰 발견

일반 캘린더 앱처럼 `09:00~10:30`이 아니라 **1교시~7교시 그리드**입니다.

- 셀이 **여러 교시에 걸쳐 병합**됨 (하자센터 에듀투어 = 1~7교시 종일)
- 여러 **날짜에 걸침** (에듀투어 11/6~11/7 이틀)
- 교시 없이 **기간만** 있는 행도 존재 (`26(월)~28(수) 과학고 원서 접수`)

→ `period_from`/`period_to` (1~7) + `all_day` 플래그가 필요.
→ 교시별 실제 시각(1교시 09:00~09:45…)은 **학교 설정**으로 따로 관리.

### 2-2. 대상이 "반 목록"으로 자유롭게 묶임

스샷에 실제로 등장하는 대상 표기:

- `마약예방교육(4,5,6반 / 1,2,3반)` — 반을 나눠 다른 시간
- `경제배움터(1~5반)` / `(6~10반)` — 범위 지정
- `쇼콜라티에 만들기(1,2교시: 앞반, 3,4교시: 뒷반)` — 앞/뒤 그룹
- `언론중재스쿨(2개반)` — 개수만
- `동양고 홍보`, `자치`, `동아리` — 전체

→ `event_target(event_id, target_type, target_id)` **다대다 필수**.
→ 반 다중 선택 UI + "1~5반" 같은 **범위 입력 단축키** 필요.

### 2-3. 색상 = 분류 (이 문서의 가독성이 곧 가치)

스샷의 색이 의미를 가집니다:

| 색 | 분류 | 예 |
|---|---|---|
| 초록 | 외부 체험·관람 | 에듀투어, 영화관람, 난타, 대학탐방, 뮤지컬 |
| 파랑 | 교과·시험 | 기말고사(사회/수학/국어/역사/영어) |
| 보라 | 진로 프로그램 | 언론중재스쿨, 고교학점제 전환기 진로 |
| 노랑/분홍 | 특별 활동 | 경제배움터, 쇼콜라티에 |
| 주황 | 휴업·전교 행사 | 대학 수능시험, 재량휴업일, 성탄절 |
| 우측 별도 | 고입 전형 | 원서접수, 면접서류 제출 |

→ `event_category(name, color)` 사용자 정의.

### 2-4. "고입 전형 일정" = 교시 그리드 밖의 **별도 레인**

우측 열은 교시와 무관한 별도 트랙입니다. 그런데 내용이 **두 성격으로 섞여 있습니다**:

- **교사 업무 마감**: `기말고사 1차 정오표`, `내신성적확인서 배부/수합`, `출결마감/나이스마감`, `기초자료확인서 배부 및 수합`, `석차연명부작성 기준일`
- **학생·학부모 안내**: `서울체고 원서접수`, `자사고 면접서류 제출`, `외고 합격자 발표`

→ 앞의 것은 **담당자·마감·개별 상태**가 붙어야 하고(= 업무 일정),
   뒤의 것은 **공지·안내**입니다(= 학사 일정).
→ 같은 레인에 표시하되 `event_type`으로 구분해야 "내 할 일"이 제대로 나옵니다.

### 2-5. 장소·시각이 제목 텍스트에 섞여 있음

`더스쿨(한성아트홀 / 11:30)`, `영화관람 (가양 롯데시네마 /11시 전 시작)`,
`난타 공연 (홍대 전용관 / 11:00)`, `창작뮤지컬 관람(도연/ 14:00/강서구민회관)`

→ `location`, `start_time`, `note` **필드로 분리**. 검색·인솔·안내문 자동생성에 필요.

### 2-6. 필요한 뷰

- [ ] **학년 일정 그리드 (날짜 × 교시)** — 스샷을 그대로 재현
      *이게 없으면 부장은 계속 엑셀을 씁니다. 1순위 화면입니다.*
- [ ] **인쇄 / PDF / 엑셀 내보내기** — 실제로 출력해서 붙이고 배포합니다
- [ ] 충돌 감지: 같은 반이 같은 날 같은 교시에 두 일정 → 경고

---

## 3. 출석부 시트에서 도출된 요구사항

### 3-1. 실제 시트 구조

```
탭: 총원 | 불참 | 3-1 | 3-2 | ... | 3-10     (반마다 탭 1개)

[3-1 탭]
       A        B        C          D          E          F
r1   3-1    참여자수     22         21         21         20
r2   연번   (담임명)  2024-12-06  2024-12-09 2024-12-10 2024-12-19
r3     1     강OO        1          1          1          1
r4     2     김OO        1          1          1          1
...   (1 = 참여, 0 = 미참여)

[총원 탭]  날짜별 전학년 참여자/미참여자 합계 + "지원 대상 3"
[불참 탭]  반 × 날짜 별 불참자 이름 목록 (없으면 "없음")
```

→ **학생(행) × 이벤트(열) 매트릭스**. 담임이 한 화면에서 반 전체를 체크합니다.

### 3-2. 여기서 나오는 기능 요구사항

- [ ] **참여 체크 그리드** — 학생 행 × 이벤트 열, 토글. `전체 참여` 버튼 후 예외만 해제
- [ ] **3단 자동 집계** — 반별 참여자 수 → 학년 총원(참여/미참여) → 불참자 명단
- [ ] **불참자 명단 뷰** — 부장용. 반별로 이름 나열, 없으면 "없음"
- [ ] **미입력 ≠ 불참 구분** ← 엑셀이 못 하는 것, 시스템의 실질 이득
      - 엑셀은 0/1뿐이라 *"담임이 아직 입력 안 함"* 과 *"전원 불참"* 이 구분 안 됩니다
      - 상태를 `미입력 / 참여 / 불참` 3종으로 두고,
        부장 대시보드에 **"3-7반 미입력"** 을 띄워야 합니다
- [ ] **불참 사유** 기록 (질병/개인사정/기타) — 현재 시트엔 사유가 없음
- [ ] **학생 플래그** — 총원 탭의 `지원 대상 3`이 그 흔적.
      참여 여부와 별개로 학생에 붙는 속성이 필요 (지원 대상, 알레르기, 차량 등)
- [ ] **결번 처리** — 3-1반 연번이 `...10, 12, 13...` 로 **11번이 없습니다**(전출).
      학번은 결번을 허용하고, 전출 학생은 삭제가 아니라 `상태 변경`으로.
- [ ] **엑셀 내보내기** — 지금 쓰는 시트 그대로의 모양으로 뽑을 수 있어야 인수인계가 됩니다

### 3-3. 참고: 이벤트별 참여율 편차가 큽니다

12/19 도자기 체험만 불참 30명(다른 날은 11~13명). 12월 말 + 교실 활동이라 그런 듯.
→ 집계는 **이벤트 단위**여야 하고, 학생 단위 누적("A학생 4회 중 1회 참여")도 볼 수 있어야
   담임이 상습 결석을 잡아냅니다.

---

## 4. 스키마 초안

```sql
-- 학교 설정: 교시 정의
period(id, school_id, no, name, start_time, end_time)   -- 1교시 09:00~09:45

-- 일정 분류(색상)
event_category(id, school_id, name, color, lane)
  -- lane: 'period_grid'(본 그리드) | 'side_track'(고입전형 같은 우측 열)

event(
  id, school_id, academic_year_id,
  title, category_id,
  event_type,            -- 'academic'(학사) | 'task'(업무)
  start_date, end_date,  -- 기간 일정
  all_day,               -- 종일이면 교시 무시
  period_from, period_to,-- 1~7
  start_time,            -- '11:30' 집합/시작 시각
  location, note,
  requires_participation, -- 학생 참여 체크 대상 여부
  owner_id, status
)

-- 대상: 전교/학년/반/부서/개인 (다대다)
event_target(event_id, target_type, target_id)

-- 교사 업무 배정 + 개인별 상태  (부장 현황판의 근거)
event_assignment(event_id, user_id, status, due_at, submitted_at, note)

-- 학생
student(id, school_id, academic_year_id, classroom_id,
        number,          -- 연번, 결번 허용
        name, status,    -- enrolled | transferred_out
        note)
student_flag(student_id, flag)      -- '지원대상', '알레르기' 등

-- 학생 참여  ← 핵심
participation(
  event_id, student_id,
  status,        -- 'pending'(미입력) | 'attended' | 'absent'
  reason,        -- 불참 사유
  updated_by, updated_at,
  primary key(event_id, student_id)
)
```

집계는 DB 뷰로 (반별 참여자 수 / 학년 총원 / 불참자 명단 / 반별 입력완료 여부).

---

## 5. 개인정보 — 이 시스템의 존재 이유

지금 방식(공유 링크로 여는 스프레드시트)은 **링크를 가진 사람이면 누구나
학년 전체 명부를 통째로 내려받을 수 있습니다.** 실제로 확인했습니다.
로그인도, 권한 검사도, 접근 기록도 없습니다.

→ 옮기기 전까지는 시트 공유 설정을 **"제한됨"** 으로 두세요.
→ 그리고 이 시스템에서는 **담임은 자기 반만, 학년부장은 자기 학년만** 보이도록
   Supabase RLS 로 DB 차원에서 강제합니다. 화면 코드의 실수로 뚫리지 않습니다.

---

## 6. 1차 개발 범위 제안 (이 두 자료 기준)

이 학교의 실제 업무를 그대로 대체하는 최소 범위:

1. 학년도 · 학년 · 반(3-1~3-10) · 학생 명단 CSV 업로드
2. 교시 설정 + 일정 분류(색상) 설정
3. 일정 등록 — 날짜/교시/대상반/장소/시각/분류/`참여체크 여부`
4. **학년 일정 그리드 뷰** (스샷 재현) + 인쇄/엑셀
5. **담임 참여 체크 그리드** (학생 × 이벤트, 전체참여 버튼)
6. **부장 참여 현황 대시보드** — 반별 참여자 수 / 총원 / 불참자 명단 / **미입력 반**
7. 권한: 담임=자기 반, 학년부장=자기 학년, 교감·교장=전교 (RLS)

댓글·첨부·알림은 2차로 미뤄도 이 흐름만으로 엑셀을 대체할 수 있습니다.
